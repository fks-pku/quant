# joinquant_small_cap_ma_stop

Keep this strategy dependent only on the strategy base/registry layer. It requires PIT market-cap fields provided by the data adapter; do not replace missing market cap with turnover or static proxies.
